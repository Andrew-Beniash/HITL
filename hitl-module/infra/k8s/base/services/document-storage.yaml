apiVersion: v1
kind: Service
metadata:
  name: document-storage
  labels:
    app.kubernetes.io/name: document-storage
spec:
  selector:
    app.kubernetes.io/name: document-storage
  ports:
    - name: http
      port: 3001
      targetPort: 3001
  type: ClusterIP
