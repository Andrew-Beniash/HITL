apiVersion: v1
kind: Service
metadata:
  name: annotation-session
  labels:
    app.kubernetes.io/name: annotation-session
spec:
  selector:
    app.kubernetes.io/name: annotation-session
  ports:
    - name: http
      port: 3003
      targetPort: 3003
  type: ClusterIP
